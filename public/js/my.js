// Generated by CoffeeScript 1.9.1
(function() {
  $(function() {
    $("#search-form").submit(function() {
      var keywords;
      keywords = $("#search").val().trim();
      location.href = "http://myphalcon2/movies/search/" + keywords;
      return false;
    });
    if ($('.next a').length) {
      key('right', function() {
        return location.href = $('.next a').attr('href');
      });
    }
    if ($('.previous a').length) {
      key('left', function() {
        return location.href = $('.previous a').attr('href');
      });
    }
    $('#favorite-button').click(function() {
      var url;
      if ($('#favorite').attr('class').trim() === 'glyphicon glyphicon-heart') {
        url = window.location.href + '/deleteFavorite';
        return $.getJSON(url, function(data) {
          if (data.status === 'success') {
            return $('#favorite').attr('class', 'glyphicon glyphicon-heart-empty');
          }
        });
      } else {
        url = window.location.href + '/addFavorite';
        return $.getJSON(url, function(data) {
          if (data.status === 'success') {
            return $('#favorite').attr('class', 'glyphicon glyphicon-heart');
          }
        });
      }
    });
    return $('#season').on('change', function() {
      var url;
      url = 'http://myphalcon2/movies/' + $(this).val();
      return location.href = url;
    });
  });

}).call(this);

//# sourceMappingURL=my.js.map
